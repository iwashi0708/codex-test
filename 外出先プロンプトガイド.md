# 外出先からのプロンプト入力ガイド（初心者向け）

スマホなど外出先からClaude Codeを使うときの「仕組み」と「困ったときの対処法」をまとめた資料です。

---

## 1. そもそもどういう仕組み？

**「クラウド上の作業用パソコンを、スマホからチャットで遠隔操作している」** イメージです。

```
   あなた（スマホ）
        │ チャットで指示
        ▼
  ☁️ クラウドの作業用PC（コンテナ）
        │ コードを書く・実行する
        ▼
  📦 GitHub（コードの保管庫）
```

あなたはチャットを打つだけ。実際の作業はクラウド上のPCの中でClaudeが行います。

---

## 2. いちばん大事な「2つのモード」の違い

外出先から使うとき、実は中身が違う2種類があります。ここを混同すると混乱します。

| | ① Claude Code on the web | ② リモートコントロール |
|--|--------------------------|------------------------|
| 作業する場所 | **クラウド上の使い捨てPC** | **あなたの手元のPC** |
| 何ができる | GitHubのコードを書く・編集 | 手元のPCのファイルを直接いじる |
| 向いている作業 | コードを書く・調べる | Unity・Blenderなど手元のアプリが必要な作業 |
| 注意点 | 手元のアプリ（Unity等）には触れない | 手元のPCの電源が入って接続中である必要あり |

👉 **Unityの揺れ設定のように「手元のアプリが必要な作業」は ② リモートコントロール です。**

---

## 3. なぜ「成果が消える」ことがあるの？

クラウドの作業用PCは **使い捨て**。しばらく使わないと電源が切れて消えます。中のファイルも一緒に消えます。

### 残したいものは必ず「GitHubに保存（push）」

- `commit`（コミット）＝変更に保存ポイントを打つ
- `push`（プッシュ）＝GitHubにアップロードする

⚠️ **push する前にPCが消えると、作業は残りません。**
（※手元PCでの作業＝リモートコントロールの場合は、ファイルは手元PCに残ります）

---

## 4. プロンプトを打とうとすると案内が出るのはなぜ？

こんなメッセージが出ることがあります：

> 🛑 リモートコントロールはオフラインです。再接続するか、`/remote-control` を実行して新しいセッションを開始してください。

### 意味
- 過去のチャット内容（ログ）は **読める**（記録は残っているから）
- でも、その会話を動かしていた **作業用PC（または手元PCの接続）が停止＝オフライン** になっている
- だから新しいプロンプトを送っても、応答する相手がいない

つまり「履歴は見られるが、続きを動かすマシンが止まっている」状態です。

---

## 5. 【解決策】次回からやること

### ケースA：手元のアプリ（Unity / Blender）を使う作業を続けたい

→ **リモートコントロールに再接続する**（手元のPCで操作）

1. 前回作業していた **自分のPC** を起動する
2. ターミナル（Mac）/ コマンドプロンプト・PowerShell（Windows）を開く
3. プロジェクトのフォルダに移動して Claude Code を起動
   ```bash
   cd （プロジェクトのフォルダのパス）
   claude
   ```
4. 入力欄に次を打って実行
   ```
   /remote-control
   ```
5. スマホのアプリでセッションを開けば、続きを送れる

> 💡 画面の「再接続」ボタンで戻れることもありますが、手元のClaude Codeが完全に終了していると上の再起動が必要です。

### ケースB：コードを書くだけでいい作業

→ 再接続は不要。**そのままクラウド側（web）で新しいセッションを始めればOK**。
ただし手元のアプリ（Unity等）には触れないので、書いたコードは後で自分で取り込む。

---

## 6. 作業を「消さない・二重にしない」ためのコツ

| やりたいこと | コツ |
|-------------|------|
| 成果を消さない | 区切りごとに **commit & push** してもらう |
| 前回の続きを正しく把握 | 手元アプリの作業は **同じPCにリモート再接続**して続ける |
| 二重作業によるエラー回避 | 新しく追加する前に、**既に同じものが無いか確認** してから取り込む |

---

## 7. 困ったときの早見表

| 症状 | 原因 | 対処 |
|------|------|------|
| プロンプトが送れない・案内が出る | セッションがオフライン | 再接続 or `/remote-control` で再開 |
| 前回の作業が見当たらない | push されていない or 一時PCが消えた | 手元PCを確認 / 次回は都度 push |
| 手元のUnity等をいじりたいのにできない | webモード（クラウド）にいる | リモートコントロールに切り替える |

---

## 参考リンク

公式ドキュメント：https://code.claude.com/docs/en/claude-code-on-the-web
